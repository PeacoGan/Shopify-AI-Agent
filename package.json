{
  "name": "shopify-ai-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MVP agent that reads product material from Feishu, drafts English Shopify product pages, and updates approved Shopify draft products.",
  "scripts": {
    "setup": "node src/setup-env.js",
    "draft": "node src/draft-products.js",
    "apply": "node src/apply-approved.js",
    "check": "for file in src/*.js; do node --check \"$file\"; done"
  },
  "engines": {
    "node": ">=20"
  }
}
